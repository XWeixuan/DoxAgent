# 浏览器批注修订验收 · 2026-09-08

范围：用户最新 23 条批注；保留既有视觉语言，依次处理全局/D1、D2、策略、事件、消息总线与配置、Runtime。预览使用 5177/8097 的隔离 mock 和真实 V2 读取 API，没有操作远端配置、workflow 或交易。

## 逐条对应

| 批注 | 实现 | 本轮实际验证 |
|---|---|---|
| 1 | Logo 在左、ticker 在右，保留唯一 Logo 返回入口 | 浏览器确认只有一个 Logo，桌面导航顺序正常 |
| 2 | D1/D2 历史左侧抽屉，按卡片展示版本概况，选择切换主阅读区 | D1 选择 research-history；D2 选择 08/18 历史，主区显示历史文档及日期，返回现行成功 |
| 3–4 | State 数值恢复普通字体；Shell → Unit → 类型 → 具体条目 | 同一 HBM Shell 含两个 Unit，切换后显示各自内容，选择 g1 定位相应 Gap；不混入另一 Unit |
| 5–6 | 全局操作区的全 Shell/全保留历史变更时间线，删除 Policy 元数据行 | 在 HBM Shell 下打开时间线仍显示 HBM 与定价两个 Policy 的正式 ADD 变更；旧 meta 行为 0 |
| 7–9 | Shell 横向滚动和边缘渐隐，与状态下拉同排分隔；KPI 数量与 LONG/SHORT 分栏 | 全部 Shell 显示 2 与 50%/50%，下拉筛选及正文切换；1262/1609px 无页面横向溢出 |
| 10–13 | Event/Fact KPI 对齐；周期移到刷新左侧；模式与状态筛选同排分隔；移除元数据行，重要标记移至标题 | 完整/新增模式，打开 E2 后标题显示重要，event-meta 数量 0 |
| 14 | 策略复用事件库的右侧隐藏索引 | 选择 SHORT 切换 P2，标题与 aria-current 同步 |
| 15 | 大型调度进度环与轮询状态重新排版 | 60s 调度下可见 31s 等实际倒计时；到期等待调度事实校准；没有为动效增加请求 |
| 16–18 | KPI 周期独立；紧凑筛选及图标搜索；提高有界流框高度 | 前一天→7 天时 KPI 11→67，同一搜索列表仍为 11 条；默认流框 640px、内部消息区约 580px，内部滚动；1262px 无横向溢出 |
| 19–20 | 标题、元数据、正式正文预览；展开继续同一 body | 可视区按需读正文首块；预览无重复报告标题/生成章节，展开显示正文；关键词 SSD 只改变列表，KPI 不变 |
| 21 | 渠道导航与配置工作区，状态概况、启用、轮询间隔、时段和参数编辑 | 对照 V1 真实配置页；表单输入同步到 JSON；无效 JSON 保留草稿并阻止保存；本地配置成功保存后重新读取值一致 |
| 22–23 | 图右侧节点详情；最近记录带表头，最终结果突出 | 1262/1609px 图与详情同顶并排；W1 统计、Case 明细；归档筛选得到 5 条且保留 W1 选择和 KPI |

## 契约与文档

- 同步 PRD Part 1 的 D2 默认范围/内部路由及策略全局时间线，Part 2 的 KPI 周期/正式正文预览/节点详情和表格布局。这些是最新明确批注对旧描述的修订。API 字段、端点、不可变版本和分页结构未变。
- 消息预览使用已有 ContentRef 的正式 body 首块，仅在卡片进入流框可视区后读取；当前端点每块 16,000 字节，不批量获取全部正文，不新增 summary。
- EventSummary 没有 is_important，当前字段仅存在于 EventDetail。标题标记在详情读取后显示并保留至同一 revision 收起；未读取详情时不推断该字段，也不批量预取全部 EventDetail。若要求所有折叠卡片首次加载即带标记，需要正式摘要契约补充此字段。
- Policy 时间线展示全局正式定义变更，不是旧 PolicySet 激活工具；不改变现行版本、Shell 或列表筛选。命中/成交不混入定义变更。
- mock 增加 D2 历史快照、多 Unit、全局 Policy ADD 历史和本地调度状态；轮询仅更新已在夹具中定义的源，不调用外部 provider。

## 验证和边界

- 20 个现有 Vitest 测试通过；最终 ESLint、类型检查、生产构建通过。构建仍提示一个入口 chunk 约 513 kB；未把包体专项优化纳入本轮。
- 浏览器检查了 1262px 和 1609px 桌面宽度的主要布局与上述交互。未进行手机验收、长历史全量压力测试、全部异常状态或真实生产链路验收。
- 验证中发现并修复了 mock 轮询误带入未注册渠道的问题。开发服务器另一次重启令浏览器落入错误页，URL 工具审核拦截继续导航；用户手动恢复后继续完成 Runtime 验证。
- 不把 mock 正常态或构建通过表述为全业务生产验收。详细旧轮次记录保留在 REDESIGN_REVIEW.md，本文件对应最新批注。

## 2026-09-08 第二轮细节批注（7 项）

- D2 删除全部内容及全部类型入口；保留 Unit → 分类 → 具体条目。侧栏明确 align-self:start，保留 sticky 和超长内容内部滚动。浏览器验证 Unit/gap URL 定位、页面滚动时索引可用。
- 策略状态移至筛选条左侧，独立右分隔线；与 Shell 横向滚动区置于同一容器。
- Delta 移除重复 summary 段落，保留变更类型、标题和版本。
- 消息源类型/发布方式移至名称下方，各来源卡片独立边框、圆角及间距。
- 折叠正文最多五行，短正文按真实长度显示；流框提高至至少 840px，继续内部滚动。底部渐隐，居中箭头与展开更多消息，保留游标分页及加载禁用。
- Runtime 最终结果、状态独立为相邻列；节点内紧凑卡片沿用原布局。
- 浏览器验证：1609/1262px 相关页面无横向溢出；消息从 20 条续页到 40 条仍保持 840px 流框；确认五行 CSS clamp、居中渐隐、正文展开、来源名称下元数据以及七列表头/数据一致。当前 mock 只有单个来源和短正文，因此真实多源堆叠和长正文内容仍须联调复核，不把 CSS 检查当成这些分支的实数据验收。
- TypeScript、ESLint、生产构建通过；构建保留约 513kB chunk 提示。本轮为展示层局部调整，未新增 API 字段、改变正文/分页契约或运行业务操作。PRD、AGENTS 和 changelog 已同步。

## 2026-09-09 策略连续阅读及第三轮批注

### 实现
- 策略默认第一个 Shell；当前 Shell 与状态筛选交集的所有已加载 Policy 连续展示。侧边索引只滚动定位，后续分页追加；每个正文保留独立错误状态、缓存及稳定锚点。
- D1 所选报告自动按顺序续取完整分块；未来节点、D2 当前 Shell 单元自动续取，移除手动继续加载报告按钮。未选择报告不预取。
- D2 实现因素单列，当前状态/实现因素/潜在差异/预期单元中文化，因素与差异字段标签加粗并加分隔。
- 共用 LoadMore 组件用于策略、事件、运行记录及对应明细、消息流：渐隐、居中箭头、追加列表。
- 消息卡片独立边框与间距；整卡非控件区域展开/收起，展开自动读取完整正文。标题保留键盘按钮，交互控件与文字选择不触发父卡片切换。
- 同步两份 PRD 和 frontend/v2/AGENTS.md，避免继续沿用单 Policy 正文要求。

### 本轮实际验证
- 内置浏览器连接真实 5178 服务：默认 ACTIVE 显示第一个 Shell 的 20 条 Policy（后端已修复此前空 ACTIVE 问题）。ALL/ADDED 范围 20→40→42 条，正文与索引一致；第二条索引定位至其正文顶部，同时全部 42 条仍在页面。
- D1 C1 正文至第六节、C3 正文 19173 字符，无手动继续加载正文；未来节点自动显示全部 26 条。
- D2 实现因素单列、字段字重 600、中文标题、无继续加载按钮，1262px 截图检查，1559px 无页面横向溢出。
- 消息正文区域点击展开、标题 Enter 收起；20→25 条追加后 message-scroll 高度 780px 不变。1559px 无页面溢出。
- 事件库20→40条、运行记录20→25条，共用居中续页。1559px 无页面溢出，Runtime 1262px 无页面溢出。
- 23 个 Vitest、ESLint、TypeScript 和生产构建通过。构建仍有现存主 chunk 超过 500KB 提示。

### 环境中断与待续验
- 切换至 D1 C5 时浏览器报告 5178 ERR_CONNECTION_REFUSED，按用户异常熔断要求停止重试，未重启服务或改动后端。
- 待恢复：C5 完整加载、策略与消息等页面两档桌面宽度补齐，以及文字选择/原始链接不误触的浏览器交互复验（保护逻辑已实现）。真实数据仅一个 Shell，多 Shell 切换尚无真实数据验收。
