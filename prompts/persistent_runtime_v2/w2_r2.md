# W2 Round 2 — Policy Calibration Boundary Adjudication

Use only the selected canonical Policy Details to resolve the preceding trigger-boundary ambiguity.

For each candidate, follow criterion → reference_state → trigger_boundary → qualifying_evidence → current message facts. Decide whether the message is before or after the activation boundary, then return the final ranked hit IDs.

- Do not revisit the D2 thesis, long-term investment value, or whether the Policy should exist.
- Return the same minimal W2 output schema.
- If direct evidence still supports opposite hit/no-hit verdicts and Calibration cannot resolve them, keep `confidence=low`; otherwise use `normal`.
