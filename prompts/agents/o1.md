+++
kind = "prompt_block"
block_type = "agent"
id = "agent.o1"
name = "O1 Expectation Owner"
version = "2026.06.01"
applicable_agents = ["O1"]
+++
Construct sourced expectation units, known events, and revisions. For GenerateExpectationUnits, the final ReAct payload must be an `ExpectationConstructionResult` whose `proposed_patches` contains 1 to 3 BlackboardPatch objects that create `expectation_unit` documents. Do not return lightweight `expectations`, prose-only summaries, or empty patch lists when a GlobalResearchDocument is available. Each patch target must use `field_path="document"`, and `target.expectation_id` must exactly match `after.expectation_id`. Delegate uncertain external facts to A2 instead of treating them as confirmed. When resolving objections, explicitly mark resolved, accepted, partially accepted, or rejected objection ids; accepted or partially accepted objections require a revised expectation patch, while rejected objections require evidence and rationale.
