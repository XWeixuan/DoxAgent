# 下一模型 handoff

这是一份会话上下文，不是任务清单；实现以同目录 `ticker_initialization_guardian_implementation_plan_20260920.md` 为准，当前代码事实以 fact pack 和接手时的 checkout 为准。

- 用户最在意的不是“尽量少写代码”，而是长达十几小时的初始化失败后能真正继续。其强烈补充是为了防止模型把“最小修复”当作困难时退出、只修眼前一处或把明显下游同源故障留给下一轮的借口。
- 粘贴基线的“两轮”和“找不到有限修复就停止”已被后续问答收紧/覆盖，不能仅阅读附件后恢复旧口径。用户已给完一轮问答，不应重新询问已定的模型、Worker/MCP 范围或预算。
- 用户说“像 real_test_issue.md 一样”的诉求是根因、文件级修复和待修事项能集中查阅，不是要求复制现有文档格式或新增 issue 管理平台。
- “main 一次读到”本轮未进一步问是 GitHub 分支页还是工作目录。方案采用工作目录阅读入口的等效解释，并明确不自动进入 main Git 历史；不要在实施时悄悄变成自动提交 main。若以后明确要求远端 Git main 版本化，那是新的边界选择。
- 本轮只获准产出方案，没有实施、部署、修真实 ticker 或登录远端的授权。后续若用户要求实施，再按该新请求行动，不把文档里的生产验收步骤当成本轮已获授权或已完成。
- 开始时仓库有用户已有的 `rklb_real_test_issue.md` 修改；fact pack 也是已有未跟踪文档。不要覆盖、清理或把它们误当本轮新产生的实现。本文及方案之外没有修改业务代码。
- 事实包来自本地勘察，不是服务器 live inventory；计划里的目录、服务和镜像接线是设计值。远端卷名、源码 revision、模型可用性与权限须在真正部署时核验，不能把本地安装 SDK 的存在当作服务器已配置。
