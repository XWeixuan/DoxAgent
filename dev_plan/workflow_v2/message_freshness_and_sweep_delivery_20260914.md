# 消息时效与 Sweep P0 修复交付记录

日期：2026-09-14。按用户要求收尾，不再扩大测试或无关修改。

## 已完成

- 修订方案：精确发布时间按普通流30分钟、Sweep固定窗口判断；日期级时间在两种流均允许今天＋昨天（ET）。完全无日期使用持久化首次发现日期，不因重抓刷新；已知旧发布时间不能被updated覆盖。
- 实现共享准入、来源时间归一、补全/发布/Runtime接收筛选、稳定编组重试、发布幂等及普通流与Sweep分离。移除无游标30天业务回退和pending sweep阻断实时抓取的逻辑。
- 定向迁移新增表，原库及冻结名单留存。事故428条中排除418条、保留10条；隔离86个Case及88条provisional的后续业务影响，保留历史事实。排除成员的模型调用数在恢复前后均为308，没有继续调用。
- 10条合法但未创建Case、被退出过程标为失败的成员已恢复PENDING并实际开始续跑，保持原接收时间及身份。
- 真实Finnhub宽窗口验收返回249条：普通流过滤249条、发布0条；Sweep过滤239条、发布10条、在隔离验收库创建10个待执行Case，模型调用0次。
- 相关用例分批通过；最终准入专项25项通过，此前编排组合30项通过；22个相关源码文件mypy通过。未进行全项目回归。
- 新加坡代码目录已同步18个发布文件。API、Bus、Enrichment、Executor等已部署最终镜像，API health返回200；已补齐镜像内官方IBKR API依赖及服务器主机映射。实时poll在Sweep继续运行时保持推进。

## 尚未完成

1. **10条合法消息的完整后台处理尚未全部结束。** 最近复核已有8条创建Case并持续推进；不宣称W1/W2/W3及后续维护全周期验收完成。
2. **Scheduler尚未切换最终统一镜像。** 它已运行核心时效/Sweep/隔离修复版本，当前镜像为`ea26c001…`；其他相关服务为最终`b18c4f69…`。为避免再次打断合法Case，本轮保留其进程，后续在合适窗口使用服务器overlay重建Scheduler即可。最终镜像已具备。
3. 本次代码未提交或推送Git；本地保留另一任务的wave/timeout改动，本次远端发布未混入这些独立功能。

## 证据与操作说明

- 事故备份及逐条清单位于远端`/data/incident-backups/message-freshness-20260914/`，含原始Runtime/Bus备份、dry-run、applied、retained-resumed及隔离真实poll结果。
- 发布源文件清单位于远端`/home/ubuntu/doxagent/.tmp/freshness-release-20260914/release.json`。
- 处置脚本：`scripts/repair_message_admission.py`。默认只读；支持幂等处置及恢复未创建Case的合法成员。
- 曾启动全库迁移，但其附带大型读模型完整校验耗时过长，已中止；最终采用本次新增表的定向迁移并验证通过。该次全库备份不能当作完成的完整校验记录。
- 本轮没有提交交易测试，没有删除原始新闻、历史模型结果或交易事实。
