diff --git a/changelog b/changelog
--- a/changelog
+++ b/changelog
@@ -305,3 +305,4 @@
 - Reworked Document2 field review orchestration so A1/C1/C3/O4 review tasks combine calibration with content supplementation, use optional single-batch tool guidance, preserve `recommended_statement` in review findings, stop fabricating review evidence refs, disable numeric-sanity blockers in review/resolver/promotion/revalidation, and raise O1 resolver budgets to 600 seconds.
 - Hardened the Document2 node-contract matrix for review/resolver robustness by adding A1 and supported supplemental findings, rejecting bad review `evidence_refs`, non-string `recommended_statement`, and patch-like review outputs, and making legacy `obj_numeric_sanity_*` objections non-actionable in resolver completion checks.
 - Added reviewer-node-only EvidenceRef acceptance layering for Document2 construction and field reviews: partial finding evidence refs are removed with working-memory warnings, bad individual findings or reviewer results are skipped without blocking other reviewers, complete EvidenceRefs remain preserved, and canonical Blackboard EvidenceRef validation stays strict.
+- Relaxed Document2 resolver transaction handling for no-op field-repair decisions so missing `changed_paths` / `evidence_refs` is audited instead of blocking the workflow, while preserving schema validation, deterministic revalidation, and blocker transition controls.
